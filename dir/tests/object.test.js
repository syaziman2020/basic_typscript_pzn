"use strict";
describe("Object", function () {
    it("should be an object", function () {
        let person = {
            id: 1,
            name: "sasya",
        };
        console.info(person.name);
        console.info(person);
    });
});
